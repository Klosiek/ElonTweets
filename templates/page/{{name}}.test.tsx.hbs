import "@testing-library/jest-dom/extend-expect";
import { render } from "test-utils";
import {{ name }} from "./{{ name }}";
import {{ name }}Mock from "./{{ name }}.mock";

it("Should render with no errors", () => {
  render(<{{ name }} {...{{ name }}Mock} />);
});
